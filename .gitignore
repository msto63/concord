# Local management files — not part of the public project
CLAUDE.md
AGENTS.md
.claude/

# Concord runtime state lives OUTSIDE the repo (per project: <repo>-coord/,
# <repo>-SESSION-SYNC.md). Never commit coordination state or session logs.
*-coord/
*-SESSION-SYNC.md

# Local archive of retired material (e.g. the original shell tools, frozen after the
# Rust migration completed). Kept on disk for reference, never published to GitHub.
.archive/

# OS / editor noise
.DS_Store
*.swp
*~

# Rust build artifacts
/target
